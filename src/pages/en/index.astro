---
/**
 * /en/index.astro - 英文首页
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import HeaderBar from '../../components/astro/HeaderBar.jsx';
import Hero from '../../components/astro/Hero.jsx';

// 导入英文 locale 数据
import en from '../../locales/en.json';

const { site, nav, header, hero } = en;

// 当前路径用于导航高亮
const currentPath = Astro.url.pathname;
---

<BaseLayout
  title={site.title}
  description={site.description}
  author={site.author}
  keywords={site.keywords}
  lang="en"
>
  <!-- HeaderBar 作为 React island，立即加载 -->
  <HeaderBar 
    client:load 
    header={header} 
    nav={nav} 
    currentPath={currentPath}
    lang="en"
  />
  
  <!-- Hero 区块 - 静态渲染（无交互） -->
  <Hero 
    subtitle={hero.subtitle}
    title={hero.title}
    description={hero.description}
  />
  
  <!-- 
    后续 PR 将添加：
    - WebsiteItem 组件
    - FeaturedPostItem 组件
    - Footer 组件
  -->
  <section class="mt-16 text-center text-gray-500 dark:text-gray-400">
    <p class="text-sm">
      🚧 PR 2 POC - 更多内容将在后续 PR 中添加
    </p>
  </section>
</BaseLayout>
